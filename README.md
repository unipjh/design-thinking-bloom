# Bloom Prototype

감정 큐레이팅 서비스 Bloom의 프로토타입입니다.

## 🚀 시작하기

### 1. 의존성 설치

먼저 Node.js가 설치되어 있어야 합니다. (권장: Node.js 14.0.0 이상)

```bash
npm install
```

Tailwind CSS도 함께 설치해야 합니다:

```bash
npm install -D tailwindcss postcss autoprefixer
```

### 2. 개발 서버 실행

```bash
npm start
```

브라우저가 자동으로 열리며 http://localhost:3000 에서 앱을 확인할 수 있습니다.

### 3. 빌드

프로덕션 빌드를 생성하려면:

```bash
npm run build
```

## 📱 기능

- **푸시 알림 시뮬레이션**: 대화 시작 화면
- **챗봇 대화**: 감정 입력 인터페이스
- **감정 꽃 시각화**: 방사형 차트로 오늘의 감정 표현
- **주간 감정 관찰**: 7일간의 감정 패턴 확인

## 🎨 기술 스택

- React 18
- Tailwind CSS
- Recharts (차트 라이브러리)

## 📁 프로젝트 구조

```
bloom-prototype/
├── public/
│   └── index.html
├── src/
│   ├── components/
│   │   ├── PushScreen.js      # 푸시 알림 화면
│   │   ├── ChatScreen.js      # 채팅 화면
│   │   ├── EmotionScreen.js   # 감정 꽃 화면
│   │   ├── FlowerChart.js     # 방사형 차트 컴포넌트
│   │   └── WeeklyScreen.js    # 주간 감정 화면
│   ├── App.js
│   ├── index.js
│   └── index.css
├── package.json
├── tailwind.config.js
└── README.md
```

## 🔧 GitHub에 올리기

```bash
git init
git add .
git commit -m "Initial commit: Bloom prototype"
git branch -M main
git remote add origin [your-github-repo-url]
git push -u origin main
```

## 📝 노트

- 이 프로토타입은 더미 데이터를 사용합니다
- 새로고침 시 상태가 초기화됩니다
- 모바일 뷰포트 (375px × 812px) 기준으로 최적화되어 있습니다
